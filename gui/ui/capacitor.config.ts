import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.servimav.jaguabit',
  appName: 'Jagua-BIT',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
